// Тема 67
// Задание 3

// Создание экземпляра (объекта) класса
let rect = new Rectangle(150,200);

// Проверка методов
console.log(rect.getWidth());
console.log(rect.getHeight());
rect.setWidth(40);
rect.setHeight(60);